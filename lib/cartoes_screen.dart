import 'package:flutter/material.dart';

class CartoesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cartões'),
      ),
      body: Center(
        child: Text('Tela de Cartões'),
      ),
    );
  }
}
